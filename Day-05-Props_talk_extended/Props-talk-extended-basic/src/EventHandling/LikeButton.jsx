import React from "react";
import Button from "./Button";
const LikeButton = () => {
  // passing handlePlayClick as the onClick prop
  return <Button>Play "{movieName}"</Button>;
};

export default LikeButton;
